image: maven:3.6.3-adoptopenjdk-15
stages:
  - test

cache:
  paths:
    - .m2/


test:
  stage: test
  script:
    - mvn test
  artifacts:
    when: always
    paths:
      - target/site/serenity/*.html
    reports:
      junit:
        - target/site/serenity/SERENITY-JUNIT-*.xml